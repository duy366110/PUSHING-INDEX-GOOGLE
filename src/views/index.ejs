<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    
    <div class="container">
        <% if (status) { %> 
            <p><%= message %></p>
        <% } %>

        <% if (status == false) { %>
            <p><%= message %></p>
        <% } %>

        <form action="/push-index" method="post">
            <input type="text" name="url" />
            <button type="submit">push index</button>
        </form>
    </div>
</body>
</html>